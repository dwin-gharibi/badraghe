darkube_build_badraghe-api-1_dngi2005-badraghe_hamravesh-c13:
  image: hamravesh.hamdocker.ir/public/darkube-cli:v1.1
  interruptible: true
  only:
    refs:
    - badraghe-API
  script:
  - IMAGE_ADDRESS="registry.hamdocker.ir/dngi2005/badraghe-api-1"
  - 'darkube build --push -t $IMAGE_ADDRESS:$CI_COMMIT_SHORT_SHA -t $IMAGE_ADDRESS:$CI_COMMIT_REF_SLUG
    --workdir . --file ./Dockerfile --build-context . '
  stage: build
darkube_deploy_badraghe-api-1_dngi2005-badraghe_hamravesh-c13:
  image: hamravesh.hamdocker.ir/public/darkube-cli:v1.1
  interruptible: true
  only:
    refs:
    - badraghe-API
  script:
  - darkube deploy --token ${DEPLOY_TOKEN_BADRAGHE_API_1_DNGI2005_BADRAGHE_HAMRAVESH_C13}
    --app-id ${APP_ID_BADRAGHE_API_1_DNGI2005_BADRAGHE_HAMRAVESH_C13} --image-tag
    ${CI_COMMIT_SHORT_SHA} --job-id ${CI_JOB_ID}
  stage: deploy
darkube_deploy_badraghe-api-2_dngi2005-badraghe_hamravesh-c13:
  image: hamravesh.hamdocker.ir/public/darkube-cli:v1.1
  interruptible: true
  only:
    refs:
    - badraghe-API
  script:
  - darkube deploy --token ${DEPLOY_TOKEN_BADRAGHE_API_2_DNGI2005_BADRAGHE_HAMRAVESH_C13}
    --app-id ${APP_ID_BADRAGHE_API_2_DNGI2005_BADRAGHE_HAMRAVESH_C13} --image-tag
    ${CI_COMMIT_SHORT_SHA} --job-id ${CI_JOB_ID}
  stage: deploy