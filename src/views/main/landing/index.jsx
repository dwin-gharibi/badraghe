import React from "react";
import {
  Box,
  Button,
  Flex,
  Heading,
  Image,
  Input,
  Select,
  SimpleGrid,
  Stack,
  Text,
  useColorModeValue,
} from "@chakra-ui/react";


export default function BaragheLanding() {
  const textColor = useColorModeValue("gray.800", "white");
  const sectionBg = useColorModeValue("secondaryGray.300", "navy.900");
  const boxBg = useColorModeValue("white", "navy.700");
  const boxTextColor = useColorModeValue("gray.600", "gray.300");

  return;
}
